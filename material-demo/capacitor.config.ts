import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webstack.material',
  appName: 'material-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
